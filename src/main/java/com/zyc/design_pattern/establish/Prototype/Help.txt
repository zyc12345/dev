1.原型模式其实就是java自带的clone（克隆）

2.要想完成克隆类必须实现Cloneable接口并且重写clone方法

3.浅克隆：克隆只是去克隆实现了Cloneable接口的方法，如果此方法中有引用存在，则克隆的是指向这个引用的指针，如果通过源对象对这个引用进行了修改，那么克隆出来的对象中引用也会发生更改。

4.深克隆：引用对象也实现Cloneable接口，然后在克隆源对象的时候将引用对象也进行克隆然后赋值给克隆后的对象引用。